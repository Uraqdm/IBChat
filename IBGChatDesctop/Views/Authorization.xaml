<Page x:Class="IBGChatDesctop.Views.Authorization"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IBGChatDesctop.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:vm="clr-namespace:IBGChatDesctop.ViewModels"
      Title="Authorization">

    <Page.DataContext>
        <vm:AuthorizationViewModel/>
    </Page.DataContext>


        <Grid>
        <StackPanel Margin="50">
            <TextBlock Text="Email" FontSize="24" Margin="5"/>
            <TextBox Text="{Binding Email}" Margin="15, 5"/>

            <TextBlock Text="Pasword" FontSize="24" Margin="5"/>
            <TextBox Text="{Binding Password}" Margin="15, 5"/>

            <Button Content="Submit" Command="{Binding Authorizate}" Margin="20"/>
        </StackPanel>
    </Grid>
</Page>
